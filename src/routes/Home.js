import React from "react";
import styled from "styled-components";

const Contaier = styled.div`
    position: absolute;
`;

const Home = () => {
    return (
        <Contaier>Home</Contaier>
    )
};

export default Home;